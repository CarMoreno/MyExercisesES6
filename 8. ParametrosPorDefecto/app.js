// Se puede establecer el valor por defecto directamente, así:
function calcularSalarioDiario(neto = 3500000, numeroHoras = 8) {
	console.log (neto, numeroHoras);
}

calcularSalarioDiario();
calcularSalarioDiario(25000000);

