// Se puede establecer el valor por defecto directamente, así:
function mostrarLenguajes(...lenguaje) {
	console.log (lenguaje);
}

mostrarLenguajes('php', 'c', 'java', 1, {}); // Retorna un array con 5 elementos

